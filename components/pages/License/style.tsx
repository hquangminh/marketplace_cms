import styled from 'styled-components';

export const LicenseComponent_wrapper = styled.div`
  .btn-add {
    position: absolute;
    top: -66px;
    z-index: 1;
    right: 20px;
  }

  .description__column {
    .render__textarea {
      white-space: pre-wrap;
      word-break: break-word;
    }
  }
`;
