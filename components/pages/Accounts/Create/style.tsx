import styled from 'styled-components';

export const styleInput = styled.div`
  position: relative;
  .wrapper__btn {
    display: flex;
    gap: 20px;
  }

  .check__role--not-allow {
    opacity: 0.7;
    pointer-events: none;
  }
`;
