import styled from 'styled-components';

export const CommentViewComponent_wrapper = styled.div`
  position: relative;

  .ant-comment-actions {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .btn__load {
    border-radius: 4px;
  }
`;
